<?xml version="1.0"?>
<ruleset name="WordPress">
	<description>The code standards for WordPress projects.</description>

	<!-- Exclude the Composer Vendor directory. -->
	<exclude-pattern>/vendor/*</exclude-pattern>

	<!-- Exclude the Node Modules directory. -->
	<exclude-pattern>/node_modules/*</exclude-pattern>

	<!-- Exclude all Javascript files. -->
	<exclude-pattern>*.js</exclude-pattern>

	<!-- Exclude minified CSS file. -->
	<exclude-pattern>style.css</exclude-pattern>

	<!-- Include the WordPress-Extra standard. -->
	<rule ref="WordPress-Extra">
	</rule>

	<!-- Add in some extra rules from other standards. -->
	<rule ref="Generic.CodeAnalysis.UnusedFunctionParameter"/>
	<rule ref="Generic.Commenting.Todo"/>
	<rule ref="Squiz.Commenting.FunctionComment.SpacingAfter"/>

	<!-- Only enforce aligning assignment operators when difference is 2 or less. -->
	<rule ref="Generic.Formatting.MultipleStatementAlignment">
		<properties>
			<property name="maxPadding" value="2" />
		</properties>
	</rule>

	<!-- Disable WordPress filename checks (we use PSR-4) -->
	<rule ref="WordPress.Files.FileName">
		<severity>0</severity>
	</rule>

	<!-- Enforce PSR-4 class filename rules -->
	<rule ref="Generic.Files.OneClassPerFile"/>
	<rule ref="Squiz.Classes.ValidClassName"/>

	<!-- Allow short array syntax (disable the sniff) -->
	<rule ref="Universal.Arrays.DisallowShortArraySyntax">
		<severity>0</severity>
	</rule>

    <rule ref="Generic.Arrays.DisallowLongArraySyntax"/>

</ruleset>
